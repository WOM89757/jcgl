
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
#     url: jdbc:mysql://172.81.209.2:3306/jcpps?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC
      url: jdbc:mysql://localhost:3306/jcpps?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC
      username: root
      password: root
#      password: W18091746128.
      max-active: 20
      max-wait: 5000
      initial-size: 1
      filters: stat,log4j,wall
      validationQuery: SELECT 'x'
      enable: true

      stat-view-servlet:
        enabled: true
        login-username: root
        login-password: root
        allow: 
        deny: 
        url-pattern: /druid/*
  #thymeleaf
  thymeleaf: 
    cache: false
    enabled: true
#      web-stat-filter: 
#        enabled: true
#        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

#  aop:
#    proxy-target-class: true
#mybatisplus
mybatis-plus:
  mapper-locations:
  - classpath:mapper/*/*Mapper.xml
  global-config:
    db-config:
      id-type: auto
    banner: true
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    
#shiro
shiro:
  hash-algorithm-name: md5
  hash-iterations: 2
  anon-urls:
  - /static/favicon.ico
  - /login/getCode*
  - /index.html*
  - /sys/toLogin*
  - /login/login*
  - /resources/**

  login-url: /index.html
  log-out-url: /login/logout*
  authc-ulrs:
  - /**

